# Copy to .env — never commit real secrets

# Required for RAG + chat
GEMINI_API_KEY=AIzaSyBLEkOIxQhL3V62K4Boyl_NECUd1D4WwWY

# Optional: faster LLM fallback experiments
# GROQ_API_KEY=

# GitHub API (public repo read; 5000 req/h)
GITHUB_TOKEN=

# Chroma persistence (default ./chroma_data)
CHROMA_PATH=./chroma_data

# RAG threshold (cosine similarity); tune if you change embedding model
SIMILARITY_THRESHOLD=0.72

# Cal.com (free tier API)
CALCOM_API_KEY=
CALCOM_BASE_URL=https://api.cal.com/v1
CALCOM_USERNAME=
CALCOM_EVENT_TYPE_ID=0

# Optional Cohere rerank (not wired in MVP)
COHERE_API_KEY=

# Optional Vapi webhook HMAC / shared secret if you add validation
VAPI_WEBHOOK_SECRET=

# Frontend (Vite) — API base for browser
VITE_API_BASE_URL=http://127.0.0.1:8000


